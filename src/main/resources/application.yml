spring:
  application:
    name: trouve_ton_match_commun

  datasource:
    url: jdbc:postgresql://db:5432/ttm_commun
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database: postgresql

logging:
  level:
    org.springframework: DEBUG
debug: true

server:
  tomcat:
    basedir: /api